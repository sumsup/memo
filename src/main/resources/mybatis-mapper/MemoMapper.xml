<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zetta.memo.page.main.MemoMapper">

    <select id="selectMemo" resultType="MemoDTO">
        select
            writer_id
            , memo
            , category
            , created_at as 'createAt'
            , updated_at as 'updateAt'
        from memo
            order by id desc
    </select>

    <insert id="insertMemo">
        insert into memo
            (
                writer_id
                , memo
                , category
                , updated_at
            )
            values
            (
                #{writerId}
                , #{memo}
                , #{category}
                , #{updateAt}
            )
    </insert>



</mapper>